// import { selectLessonPlanPageDomain } from '../selectors';

describe('selectLessonPlanPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
