// import { selectRegisterUserDomain } from '../selectors';

describe('selectRegisterUserDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
