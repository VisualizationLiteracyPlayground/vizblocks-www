// import { selectVlatCategoryPageDomain } from '../selectors';

describe('selectVlatCategoryPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
