// import { selectAccountSettingDomain } from '../selectors';

describe('selectAccountSettingDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
