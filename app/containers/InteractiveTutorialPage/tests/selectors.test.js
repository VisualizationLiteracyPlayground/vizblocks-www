// import { selectInteractiveTutorialPageDomain } from '../selectors';

describe('selectInteractiveTutorialPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
